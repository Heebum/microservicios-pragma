spring:
  application:
    name: eureka-service

  cloud:
    config:
      uri: http://localhost:8081
      username: root
      password: s3cr3t
      default-label: main
#      enabled: true


logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} ${LOG_LEVEL_PATTERN:-%5p} %m%n"
